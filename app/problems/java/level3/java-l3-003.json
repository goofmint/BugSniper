{
  "id": "java-l3-003",
  "codeLanguage": "java",
  "level": 3,
  "code": [
    "import java.util.List;",
    "import java.util.stream.Collectors;",
    "",
    "public class StreamExample {",
    "    public List<String> processNames(List<String> names) {",
    "        return names.stream()",
    "            .filter(name -> name != null)",
    "            .map(name -> name.toUpperCase())",
    "            .collect(Collectors.toList());",
    "    }",
    "}"
  ],
  "issues": [
    {
      "id": "java-l3-003-1",
      "lines": [6],
      "type": "bug",
      "severity": "critical",
      "score": 4,
      "description": {
        "ja": "NullPointerExceptionが発生する可能性があります。namesリスト自体がnullの場合、stream()を呼び出すと例外が発生します。使用前にnullチェックが必要です。",
        "en": "Potential NullPointerException. If names list itself is null, calling stream() throws exception. Need null check before use."
      }
    }
  ]
}
