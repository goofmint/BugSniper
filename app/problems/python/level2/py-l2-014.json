{
  "id": "py-l2-014",
  "codeLanguage": "python",
  "level": 2,
  "code": [
    "def merge_sets(set1, set2):",
    "    result = set1",
    "    result.update(set2)",
    "    return result"
  ],
  "issues": [
    {
      "id": "py-l2-014-1",
      "lines": [2],
      "type": "bug",
      "severity": "normal",
      "score": 3,
      "description": {
        "ja": "セットは参照でコピーされるため、元のset1が変更されます。'set1.copy()'を使うべきです",
        "en": "Sets are copied by reference, modifying original set1. Should use 'set1.copy()'"
      }
    }
  ]
}
