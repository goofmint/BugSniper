{
  "id": "py-l3-017",
  "codeLanguage": "python",
  "level": 3,
  "code": [
    "class AsyncResource:",
    "    def __aenter__(self):",
    "        self.resource = await acquire_resource()",
    "        return self.resource",
    "    ",
    "    async def __aexit__(self, exc_type, exc_val, exc_tb):",
    "        await self.resource.close()"
  ],
  "issues": [
    {
      "id": "py-l3-017-1",
      "lines": [2, 3, 4],
      "type": "bug",
      "severity": "critical",
      "score": 4,
      "description": {
        "ja": "__aenter__メソッドは'async def'で定義する必要があります",
        "en": "__aenter__ method must be defined with 'async def'"
      }
    }
  ]
}
